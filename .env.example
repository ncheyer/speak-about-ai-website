# CAPTCHA Configuration

This site uses Cloudflare Turnstile for spam protection on public forms.

Environment variables required:
- NEXT_PUBLIC_TURNSTILE_SITE_KEY (public site key for frontend)
- TURNSTILE_SECRET_KEY (secret key for backend verification)

